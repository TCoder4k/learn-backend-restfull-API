version: "3.8"

services:
  db-mysql:
    image: mysql:8.0
    restart: always

    environment:
      MYSQL_DATABASE: tcoder4k
      MYSQL_ROOT_PASSWORD: 123456

    ports:
      - "3307:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
